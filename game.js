// Guess the Number Game:
// Use numbers for the target number.
// Use booleans to track if the user has guessed correctly.
// Use strings to provide feedback.

let maxNum =10;
let guessNum =5;
if(guessNum === maxNum){
    console.log("The user guessed right");
} else{
    console.log("The user guessed wrongly")
}