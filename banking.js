// Use objects to represent accounts.
// Use numbers for balances.
// Use booleans to check for active accounts.
let acct1 ={
    balance: 5000,
    isActive: true,
}
let acct2 ={
    balance: 10000,
    isActive: true,
}
