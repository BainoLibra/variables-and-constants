// Calculate simple interest using a fixed rate.
const INTEREST_RATE =5/100;
let principle = 1000;
let time =2;
let interest = principle * INTEREST_RATE * time;
console.log (`The interest for $${principle} over ${time} years is $${interest}`)